import TextInput from '../../../UI/TextInput/TextInput'

const simpleFormRecipe = [
    {
        component: TextInput,
        name: 'username',
        address: ['username'],
        props: {
            autoComplete: 'off'
        }
    }
]

export default simpleFormRecipe
